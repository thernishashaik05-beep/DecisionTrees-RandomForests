# DecisionTrees-RandomForests
This project implements Decision Tree and Random Forest classifiers on the Heart Disease dataset. It includes tree visualization, overfitting control using max depth, model comparison, feature importance analysis, and 5-fold cross-validation to evaluate performance and understand tree-based machine learning models.
